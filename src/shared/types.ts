/**
 * Shared runtime shape notes for the JavaScript-compatible TypeScript source.
 *
 * RiskLevel: "safe" | "caution" | "high"
 * Sensitivity: "low" | "normal" | "high"
 * MatchedSignal: { id, category, label, evidence, weight, explanation }
 * RiskAnalysisResult: { riskLevel, score, matchedSignals, explanation, recommendedActions }
 * SilverShieldSettings: { trustedContactName, trustedContactContact, sensitivity, warningBannersEnabled, emergencyHelpText }
 */
export {};
